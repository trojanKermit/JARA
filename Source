<!DOCTYPE html>
<html lang="no">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Iphone test</title>
  <style>
    body {
      background-color: #111;
      color: #00ff00;
      font-family: -apple-system, BlinkMacSystemFont, Arial, sans-serif;
      padding: 20px;
      margin: 0;
    }

    .form-row {
      display: flex;
      flex-direction: row;
      align-items: center;
      margin-bottom: 12px;
      flex-wrap: wrap;
    }

    .fixed-column {
      width: 150px;
      min-width: 120px;
      background-color: #222;
      padding: 8px;
      margin-bottom: 5px;
      text-align: right;
      user-select: none;
      color: #00ff00;
      flex-shrink: 0;
    }

    .input-column {
      flex: 1;
      min-width: 0;
    }

    input[type="text"] {
      width: 100%;
      padding: 10px;
      font-size: 16px;
      background-color: #333;
      color: #00ff00;
      border: 1px solid #00ff00;
      border-radius: 4px;
    }

    #summary {
      margin-top: 20px;
      padding: 12px;
      border: 1px solid #00ff00;
      background-color: #222;
      white-space: pre-wrap;
      color: #00ff00;
      border-radius: 4px;
      font-size: 16px;
    }

    button {
      margin-top: 15px;
      padding: 12px 20px;
      font-size: 16px;
      background-color: #000;
      color: #00ff00;
      border: 1px solid #00ff00;
      border-radius: 4px;
      cursor: pointer;
      width: 100%;
    }

    button:hover {
      background-color: #222;
    }

    @media (max-width: 480px) {
      .form-row {
        flex-direction: column;
        align-items: stretch;
      }

      .fixed-column {
        text-align: left;
        padding-left: 0;
      }

      button {
        font-size: 18px;
      }
    }
  </style>
</head>
<body>
  <form id="myForm" autocomplete="off">
    <!-- Custom Labels -->
    <div class="form-row">
      <div class="fixed-column">Situasjon:</div>
      <div class="input-column"><input type="text" name="Situasjon" autocomplete="off" /></div>
    </div>
    <div class="form-row">
      <div class="fixed-column">Oppdrag:</div>
      <div class="input-column"><input type="text" name="Oppdrag" autocomplete="off" /></div>
    </div>
    <div class="form-row">
      <div class="fixed-column">Utførelse:</div>
      <div class="input-column"><input type="text" name="Utførelse" autocomplete="off" /></div>
    </div>
    <div class="form-row">
      <div class="fixed-column">ADM&amp;FOR:</div>
      <div class="input-column"><input type="text" name="ADM&FOR" autocomplete="off" /></div>
    </div>
    <div class="form-row">
      <div class="fixed-column">LED&amp;SAM:</div>
      <div class="input-column"><input type="text" name="LED&SAM" autocomplete="off" /></div>
    </div>

    <!-- Summary and Copy -->
    <div id="summary" aria-live="polite">Summary will appear here...</div>
    <button type="button" id="copyBtn">Copy Summary to Clipboard</button>
  </form>

  <script>
    const form = document.getElementById('myForm');
    const summary = document.getElementById('summary');
    const copyBtn = document.getElementById('copyBtn');

    function updateSummary() {
      const fields = ['Situasjon', 'Oppdrag', 'Utførelse', 'ADM&FOR', 'LED&SAM'];
      let text = '';

      fields.forEach(label => {
        const value = form.elements[label].value.trim();
        if (value) {
          text += `${label}: ${value}\n`;
        }
      });

      summary.textContent = text || 'Summary will appear here...';
    }

    form.addEventListener('input', updateSummary);

    copyBtn.addEventListener('click', () => {
      const textToCopy = summary.textContent;
      if (!textToCopy || textToCopy === 'Summary will appear here...') {
        alert('No summary to copy!');
        return;
      }

      if (navigator.clipboard && navigator.clipboard.writeText) {
        navigator.clipboard.writeText(textToCopy)
          .then(() => alert('Summary copied to clipboard!'))
          .catch(() => fallbackCopy(textToCopy));
      } else {
        fallbackCopy(textToCopy);
      }
    });

    function fallbackCopy(text) {
      const textarea = document.createElement('textarea');
      textarea.value = text;
      document.body.appendChild(textarea);
      textarea.select();
      try {
        document.execCommand('copy');
        alert('Summary copied to clipboard!');
      } catch (err) {
        alert('Clipboard copy failed.');
      }
      document.body.removeChild(textarea);
    }

    updateSummary();
  </script>
</body>
</html>
